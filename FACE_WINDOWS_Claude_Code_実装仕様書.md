# FACE WINDOWS — Claude Code向け実装仕様書

> カメラに映った人物の顔が、デスクトップ上の**大量の小さなウィンドウ**へリアルタイムに分裂するインタラクティブアート。
>
> **最重要：スピード、大量生成、顔の動きと映像への反応、右側の管理ウィンドウ。**

## 0. Claude Codeへの依頼と進め方

この仕様をもとに、現在の開発環境を調査し、技術選定から実装・起動・動作確認・改善まで進めてください。**設計書やモック画面だけで終了せず、実際にカメラを使って動くアプリケーションを作ること。**

- プログラミング言語、フレームワーク、GUIライブラリ、画像認識モデルは**指定しない**。Unityを使う前提にも、Python・MediaPipe・YOLOを使う前提にもしない。動作環境、性能、開発効率を比較して選定する。
- 「参考映像を忠実に複製すること」ではなく、「以下の観察から定義した体験・要件を実装すること」が目的。映像の内部実装は不明なので推測を事実として扱わない。
- 参考資料として `https://github.com/cverrier/segcam/tree/main` を挙げるが、利用必須ではない。このリポジトリをFACE WINDOWS本体のソースコードだと決めつけない。
- 不明点は合理的な初期値を選び、管理画面で後から調整可能にする。進行を止める重要な権限・OS制約・ハードウェア制約だけ質問する。
- 既存プロジェクトへ追加する場合は、先に現状を調査し、既存ファイルや動作を不用意に破壊しない。

## 1. 作品の体験・コンセプト

ユーザーがWebカメラに顔を映すと、顔全体・目・鼻・口などの**ライブ映像の切り抜き**が、小さなタイトルバー付きウィンドウに表示される。それらが瞬時に増殖し、重なり、散らばり、動き、顔の移動や表情に反応する。画面内には同じ目や口を表示するウィンドウが複数同時に存在してよい。静止した5個程度のパーツを整然と並べるだけの作品にはしない。

**スピードと視覚的な密度が中心的価値。** 数十〜数百個規模の表示を設定・試行できることを目指し、実機の限界に応じて適応制御する。数百個の独立したOSウィンドウで常時高FPSを保証することは要求しないが、負荷を理由に最初から少数固定にしない。

「動きを検知した領域からウィンドウが生まれる」「古い映像の小窓が少し残る」「大量に出ては消える」など、実際の顔や身体の動きとウィンドウの挙動が結び付いた演出を用意する。

## 2. 必須の画面構成

### 2.1 管理ウィンドウ（CONTROL PANEL）— 必須

- 起動時に独立した管理ウィンドウを表示する。参考映像のように**画面右側への配置を初期値**とする。任意位置への移動・リサイズも可能。
- 顔パーツウィンドウの生成が大量になっても、操作できる状態を優先する。管理ウィンドウは前面固定のON/OFFを持つ。
- 設定を変更したら、可能な限り**再起動なしで実際の映像・ウィンドウ挙動に反映**する。
- カメラプレビュー、現在の検出状態、実際に稼働中のウィンドウ数、入力FPS・推論FPS・表示FPS、処理時間、メモリ使用量、警告を表示する。取得できない指標は推定値を装わず「未計測」と表示する。
- START / PAUSE / STOP（すべての作品用ウィンドウを閉じてカメラ処理も停止）/ RESET を設ける。管理ウィンドウの終了はアプリ全体の安全な終了へつなげる。
- 接続可能なカメラ選択、解像度・入力FPS選択、左右反転、検出状態のプレビューを用意する。
- 設定の保存・読込、初期化、演出プリセットの保存・切替を用意する。

### 2.2 作品用ウィンドウ群

- 顔全体・左目・右目・鼻・口など、**選択した検出対象のライブ映像**を切り出して表示する。
- 顔パーツ単位に1ウィンドウで終わらず、同じ切り抜きから複数のウィンドウを生成する。
- 初期スタイルは**OSの小さなアプリウィンドウ風（タイトルバーと枠が見える）**。サイズ・重なり・生成位置を変える。フレームレス表示は追加オプションとしてよい。
- 作品用ウィンドウと管理ウィンドウは機能的に分離し、作品用の個別ウィンドウを閉じても全体アプリが落ちない。
- 背景のデスクトップが見え、顔の映像窓がそこへ浮かぶ表現を維持する。

### 2.3 ウィンドウの実装方式の判断

参考映像は個別のOSウィンドウに見えるが、内部実装は未確認。**見た目と生成・移動の速さを両立**させることを優先する。

1. まず独立したOSウィンドウの生成・再利用・移動を検証する。
2. OSウィンドウを大量生成すると速度や操作性が極端に落ちる場合、GPU描画による「デスクトップ上に浮かぶ大量のウィンドウ風UI」等を代替案として検証してよい。
3. 代替方式でも、管理画面との分離、背景デスクトップの見え方、タイトルバー付きの重なり、移動・増殖の印象を維持する。
4. **実際のOSウィンドウか、描画による疑似ウィンドウかは、管理画面・READMEで明記**する。疑似ウィンドウをネイティブウィンドウと説明しない。
5. 方式選択前に小規模な性能比較を行い、選定理由と測定値を残す。混合方式も可。

## 3. TRACKING — 「どこを見るか」を設定できること

管理画面に **TRACKING / Face & Body** セクションを必ず設ける。参考映像に似た名称を採用してよいが、細部の動作はこの仕様に従う。各対象は個別にON/OFF可能。すべてOFFなら新規生成しない。

| 項目 | 想定する検出・切り抜き対象 | 初期値 |
|---|---|---|
| Face | 顔全体 | ON |
| Left eye / Right eye | 左右の目。別々に有効化可能 | ON |
| Nose | 鼻 | ON |
| Mouth | 口 | ON |
| Body | 人体全体。背景からの切り抜きが可能なら使用 | OFF |
| Hands / Arms / その他 | 実際に安定検出できる場合の拡張 | OFF |

- `Face & Body` は設定グループ名とし、FaceとBodyをひとつのトグルに強制しない。
- **Threshold / Sensitivity**：検出信頼度や動き判定のしきい値を調整する。何のしきい値か明示し、検出信頼度とモーション感度は必要なら別スライダーにする。
- **Motion**：映像内の動きを生成トリガーとして使うON/OFF。ON時は顔・身体の位置/動き量に応じて生成量・方向・散らばりを変化させる。OFFでも検出対象を映し続ける通常生成モードは利用可能。
- 検出した対象領域と切り抜き枠を、管理画面のプレビュー上に重ねて確認できる。追跡に失敗した対象はその旨を示す。
- 画像の左右反転、人物から見た左右、画面座標を混同しない。
- 身体や手などの追加検出は顔の高速描画を阻害しないよう個別にON/OFFできる。使用するモデル・検出器は固定しない。

## 4. 生成・動き・映像の仕様

### 4.1 生成（GENERATION）

- 最大同時表示数：UIから変更可能。初期例100、調整範囲は実装可能な範囲で広くする。
- 生成レート：個/秒で指定できる。初期例30個/秒。瞬間的に複数同時生成する**BURST**も実装する。
- ウィンドウ寿命：秒単位で変更でき、終了後はフェード/縮小/消失またはプールへ返却する。
- 上限到達時は古いウィンドウを再利用するなどし、操作不能な無限増殖を避ける。
- 生成位置：顔中心の周囲、検出部位付近、画面内ランダム、直前の動きの方向、から選べる。重なりは演出として許可。
- サイズ・サイズ揺らぎ、同じパーツの複製比率、顔全体と部分の出現比率を調整できる。

### 4.2 動き（MOTION / ANIMATION）

- モード：追従、飛散、ランダム漂流、位置固定、混合。
- 顔全体の移動・向き・動きの強さに応じて各ウィンドウの移動先や生成量を変える。
- 各ウィンドウは独立した速度・方向・追従遅れ・寿命を持てる。同じパーツが同期して一枚の塊になるだけにはしない。
- 移動速度、追従速度、散らばり、ランダム性、加速・減衰をリアルタイムで調整できる。
- デスクトップ外へ飛び続ける、完全に回収不能になる動作は避ける。画面端で跳ね返り/消滅/再配置などを選べる。

### 4.3 表示映像（IMAGE）

- 初期設定は**リアルタイム切り抜き**。同一カメラフレームを各窓で個別解析しない。
- 静止画スナップショット、指定した秒数だけ過去の映像、最終検出フレーム保持は追加モードとして実装してよい。
- パーツの切り抜きが失敗したフレームでは、破損画像を出さず短時間の最終映像保持または自然な消失に切り替える。
- 検出座標・入力映像座標・画面座標・表示DPIを整理し、顔の移動に合わせて意図した位置へ描画する。

## 5. 管理画面の推奨セクション

| セクション | 調整・表示項目 |
|---|---|
| CAMERA | デバイス、解像度、FPS、左右反転、ライブプレビュー |
| TRACKING / Face & Body | Face / Eyes左右 / Nose / Mouth / Body、しきい値、モーション検出、プレビュー上の検出枠 |
| GENERATION | 最大数、生成レート、BURST数、寿命、出現範囲、パーツ比率 |
| MOTION | モード、速度、追従遅延、飛散、ランダム性、画面端の動作 |
| DISPLAY | タイトルバー風/フレームレス、サイズ、重なり、透明度、最新映像/残像 |
| PERFORMANCE | 現在数、カメラFPS、推論FPS、描画FPS、遅延、CPU/GPU/メモリ（取得可能なもの）、自動負荷調整 |
| PRESETS / SYSTEM | プリセット、保存/読み込み、START/PAUSE/STOP/RESET、終了 |

管理画面は操作を探しやすいタブ・折りたたみ構成とし、頻繁に変える「数・生成速度・移動速度・検出対象」は少ない操作でアクセスできるようにする。

## 6. 性能要件と設計上の優先順位

**優先順位：** ①管理画面と緊急停止の操作性 ②カメラ入力と検出の低遅延 ③大量ウィンドウの高速増殖・移動 ④演出の豊かさ。

- カメラ取得・検出・画像処理・表示を必要に応じて分離する。処理落ち時に古いフレームが無制限にキューへ蓄積しないよう、最新フレーム優先を基本とする。
- 顔/身体の検出は入力フレームを基点に一度実行し、結果を複数のウィンドウで共有する。同じ顔の目を100個の窓それぞれで認識し直さない。
- 画像切り抜き・リサイズ・転送・描画のコストを計測し、キャッシュ、バッチ処理、GPU使用、ウィンドウ再利用など適切な方式を選ぶ。
- **30fps入力を初期目標**とし、実機での実測値を表示する。推論/描画は実環境次第なので達成値を捏造しない。
- 20→100→200個など表示規模を上げて計測し、生成速度、実FPS、遅延、メモリ、GUI応答性を記録する。100/200個が不可能なら理由と代替方式の結果を示す。
- 性能不足時は、解析フレーム間引き、遠方・小窓の更新頻度低下、古いウィンドウの再利用、画質調整、生成レート制限などで自動調整する。上限は設定可能とし、安全な初期値を採用する。
- ウィンドウ数と生成速度のスライダーの値は**要求値**と**実際の値**を区別し、システムが自動抑制したときは管理画面に表示する。

## 7. 操作・安全・プライバシー

- 管理ウィンドウと作品用ウィンドウが重なっても、終了操作を失わないようにする。
- キーボードからの緊急停止を用意し、管理画面にも常時見えるSTOPボタンを置く。グローバルショートカットの実装がOSや権限で困難な場合、実際に機能する代替終了手段を説明する。
- STOP時は新規生成を即座に止め、作品用ウィンドウを回収してカメラを解放する。異常終了時にも残存ウィンドウをできるだけ防ぐ。
- 初期設定では入力映像をディスクへ保存・外部送信しない。録画/スナップショットの保存を導入する場合は明示的な操作と保存先を用意する。
- カメラ利用権限拒否、未接続、対象が検出できない場合は管理画面で状況を説明し、落ちずに復旧・再試行できるようにする。
- 必要な範囲の画面・カメラアクセス権限のみ要求する。

## 8. 作業順序（段階ごとに動くものを残す）

1. **環境調査・方式の短い比較**：OS、カメラ、GPU、DPI、必要な権限、OS実ウィンドウ方式と高速描画方式の成立性を確認。`TECH_DECISIONS.md` に採用理由を記録。
2. **最小動作**：カメラ→顔検出→顔のライブ切り抜きを1ウィンドウへ表示。管理ウィンドウでSTART/STOP可能にする。
3. **大量生成**：顔全体・目・鼻・口を複製、複数表示、生成数と速度を調整、負荷時に再利用できるようにする。
4. **追従と演出**：顔の動きに応じて追従・飛散・増殖・消滅。低遅延を確認。
5. **TRACKING / Face & Body**：対象別ON/OFF、しきい値、Motion、Body任意追加。プレビューの検出領域表示。
6. **管理画面の仕上げ**：すべての主要パラメータをライブ操作、プリセット保存、統計、緊急停止。
7. **測定・調整**：20/100/200個などで測定しボトルネックを改善。デモの見た目と速度を優先して初期値を調整。

## 9. 受入条件（デモで確認する）

- [ ] カメラに顔を映すと、顔全体・目・鼻・口の**実際のライブ映像**が小窓に表示される。
- [ ] 1部位から複数の窓が高速に生まれ、少数固定ではなく、設定に応じて大量に表示・移動・消滅する。
- [ ] 顔を左右に動かす・口を開く等の入力が、表示映像またはウィンドウの挙動へ目視できる遅延の少ない形で反映される。
- [ ] 右側の管理ウィンドウに、検出プレビュー、Face & Bodyの対象選択、Motion/Threshold設定、生成数・速度・サイズ・寿命、動きの設定がある。
- [ ] 管理画面から設定を変更すると、再起動せず作品へ反映される。
- [ ] ウィンドウ大量生成中も管理画面のSTOPとアプリ終了が機能する。
- [ ] 実際の表示数、入力/推論/表示FPSなど測定可能な性能が見られ、負荷上限を超えても無限増殖しない。
- [ ] 初回セットアップ手順、起動手順、必要なモデル/権限、既知の制約をREADMEに記載する。
- [ ] OS実ウィンドウか疑似ウィンドウか、実際の測定条件・測定結果を明示する。

## 10. 納品物

- 実行可能なアプリケーションのソースコード一式
- 初回導入・起動・終了・設定手順を記した `README.md`
- 技術選定と方式比較を記した `TECH_DECISIONS.md`
- パラメータの初期値と保存機構、演出プリセット（例：高速増殖 / 顔追従 / ランダム飛散）
- 可能な範囲の自動テスト、実機での性能測定結果、未実装項目・既知の問題

**ゴール：管理ウィンドウを操作しながら、カメラ映像の顔が大量の小さなウィンドウとしてデスクトップ上で高速に分裂・増殖する、実際に触って遊べる作品を完成させる。**
