# TECH_DECISIONS — FACE WINDOWS

## 1. 環境調査（2026-09-24 実機）

| 項目 | 内容 |
|---|---|
| OS | Windows 11 Pro 10.0.26200 |
| GPU | NVIDIA GeForce RTX 5070 Laptop (8GB) + AMD Radeon 890M |
| 画面 | 内蔵 2880×1800（150%スケール、論理 1920×1200）＋ 外部 DELL S2725DS 2560×1440 |
| カメラ | ASUS FHD webcam（1280×720 / 30fps 取得を確認）、NVIDIA Broadcast / OBS 仮想カメラ |
| Python | 3.10 / 3.11 / 3.13 / 3.14 が導入済み → MediaPipe の対応がある **3.11** を採用 |
| 権限 | Windows のカメラ使用許可のみ。グローバルホットキーは管理者権限不要（RegisterHotKey） |

## 2. 採用技術

| 役割 | 採用 | 理由 |
|---|---|---|
| 言語 | Python 3.11 | 顔検出ライブラリと GUI が揃い、試作→調整が最速。重い処理は C++ 実装のライブラリに任せる |
| 顔・身体検出 | MediaPipe Tasks（FaceLandmarker / PoseLandmarker lite / HandLandmarker） | 顔 478 点で目・鼻・口を安定して切り出せる。実測 **顔 5〜6 ms/フレーム（CPU）**。Pose はセグメンテーションマスクも出すので Body を背景透過で切り抜ける |
| カメラ・画像処理 | OpenCV（DirectShow + MJPG） | 最新フレームのみ保持、`warpAffine` で「回転補正＋切り抜き＋縮小」を1回で行う |
| GUI | PySide6（Qt 6.11） | 透明・クリック透過・最前面のウィンドウ、OS 実ウィンドウ、管理画面をひとつのツールキットで作れる |
| プロセス構成 | GUI プロセス ＋ 検出プロセス（multiprocessing spawn） | 下記 4. 参照。GIL の取り合いで推論が 30→10fps に落ちたため分離 |

Unity / YOLO 等も候補にしたが、「顔パーツを細かく切り出す」には顔ランドマークの方が向く（YOLO は顔の矩形まで）。Unity は透明オーバーレイ＋別の管理ウィンドウという構成が Windows では手間が多い。

## 3. ウィンドウ方式の比較（仕様書 §2.3）

`bench/window_bench.py`（ダミー映像 160×120 を N 個、毎フレーム移動＋再描画、内蔵 2880×1800 画面）:

| 方式 | 20個 | 60個 | 100個 | 200個 |
|---|---|---|---|---|
| OS 実ウィンドウ（Qt `move`） | 49 fps | – | 8.2 fps | 2.2 fps |
| OS 実ウィンドウ（Win32 `DeferWindowPos` 一括移動） | 98 fps | 24 fps | 12.7 fps | – |
| **疑似ウィンドウ（1枚の透明オーバーレイに描画）** | 348 fps | – | 183 fps | **105 fps** |

**結論：既定は疑似ウィンドウ方式（overlay）**。OS 実ウィンドウは 1 枚ごとに DWM の合成・メッセージ処理が入り、数十個で実用限界になる。
ただし「本物の OS ウィンドウ」の手触りも残すため、以下を切替可能にした（管理画面 DISPLAY → 描画方式）。

- `overlay` 疑似ウィンドウのみ（既定・大量向け）
- `native` OS 実ウィンドウのみ（「実ウィンドウ上限」まで。既定 24）
- `hybrid` 上限までは OS 実ウィンドウ、超えた分は疑似ウィンドウ

疑似ウィンドウは OS のウィンドウではない（クリック・ドラッグ不可、クリックは下のデスクトップへ透過）。管理画面の上部と README に明記している。

## 4. 性能上の設計判断と、その根拠になった測定

1. **検出はフレームにつき1回、結果を全ウィンドウで共有**。パーツごとに 240px の切り抜きを1枚作り、全窓がそれを描画時に拡大縮小する。
2. **最新フレーム優先**：カメラスレッドは最新1枚だけ保持、検出は最新だけを処理、プロセス間も最新スナップショットのみ送る（キューに溜めない）。
3. **検出を別プロセスへ**：同一プロセスのスレッドだと、200 窓描画中に推論が **29.8 → 10〜12 fps**、遅延 **170 ms** まで悪化（GIL 競合）。別プロセス化後は 200 窓でも推論 **29.7 fps**、遅延 **53〜58 ms**。
4. **画像形式**：切り抜きを最初から BGRA（Qt の `Format_RGB32` / `ARGB32_Premultiplied`）で作る。RGB888 は描画のたびに変換が入り遅い（100 窓で描画 18→14 ms）。
5. **窓の枠**：窓全体サイズの半透明ピクスマップを重ねるとアルファ合成が重いので、タイトルバーだけを不透明な画像にし、枠線・影は線で描く（100 窓で 14→9 ms）。
6. **OS 実ウィンドウの再利用**：`hide/show` は 1 回 約 7 ms、`resize` は 約 2.6 ms。消える窓のウィンドウを隠さずに次の窓へ引き継ぎ、サイズも変えない（映像は中央を切り出して合わせる）。24 窓で **10 → 44 fps**。
7. **自動負荷調整**：描画 FPS が目標（既定 30）の 85% を下回ると実上限を 12% ずつ下げ、余裕が出たら戻す。要求値と実際の上限を管理画面に並べて表示。

## 5. 測定結果（アプリ内ベンチマーク）

条件：内蔵ディスプレイ 2880×1800（DPR 1.5）、1280×720 入力、スタイル=タイトルバー風（ライト）、影あり、窓サイズ 150px±45%、生成 300/s（上限到達後は古い窓を再利用し続ける＝最も負荷の高い状態）。3 秒の立ち上げ後 3 秒間を測定。`bench/results/*.json` に保存される。

**実カメラ（ASUS FHD webcam、顔を映した状態）・overlay**

| 要求数 | 実際の数 | 描画 FPS | 1フレーム処理 | うち描画 | カメラ→表示 遅延 | 入力 FPS | 推論 FPS |
|---|---|---|---|---|---|---|---|
| 20 | 20 | 67.0 | 6.3 ms | 2.5 ms | 37 ms | 29.7 | 29.7 |
| 100 | 100 | 53.7 | 17.3 ms | 13.1 ms | 51 ms | 29.8 | 29.7 |
| 200 | 200 | 32.8 | 29.1 ms | 24.8 ms | 57 ms | 29.8 | 29.8 |

**静止画入力（`--source`、再現性のため）**

| 方式 | 要求数 | 実際（うち実ウィンドウ） | 描画 FPS | 遅延 | 推論 FPS |
|---|---|---|---|---|---|
| overlay | 20 / 100 / 200 / 300 | 20 / 100 / 200 / 300 | 67.0 / 60.2 / 36.4 / 26.0 | 37 / 47 / 58 / 68 ms | 29.7 |
| native | 10 / 24 | 10 (10) / 24 (24) | 67.0 / 44.4 | 34 ms | 29.7 |
| hybrid | 100 / 200 | 100 (24) / 200 (24) | 34.3 / 24.1 | 44 / 53 ms | 29.7 |

メモリ：描画プロセス 約 320〜330 MB（＋検出プロセス。管理画面の PERF に両方表示）。
描画 FPS の上限 67 はタイマー間隔（15 ms）による。GPU 使用率は取得していない（管理画面では「未計測」と表示）。

- 200 個は 30fps 以上で実用。300 個は約 26fps（自動負荷調整を ON にすると目標 30fps に向けて実上限が下がる）。
- OS 実ウィンドウは 24 個で約 44fps、それ以上は hybrid で疑似ウィンドウと混ぜる。

## 6. 今後の改善候補

- 疑似ウィンドウの描画を GPU（OpenGL/D3D の透明ウィンドウ）へ移すと 300〜500 個以上が狙えるが、Windows の透明 GL ウィンドウは環境依存が大きいので今回は Qt のラスタ描画に留めた。
- 画面全体（2880×1800）の透明ウィンドウの転送が固定費になっている。窓が集中している範囲だけを更新すると改善の余地がある。
